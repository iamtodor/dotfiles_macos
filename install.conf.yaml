- defaults:
    link:
      relink: true
    shell:
      stdout: true
      stderr: true


- clean: ['~']

- link:
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
   ~/.zshrc: zshrc
   ~/.gitconfig: gitconfig
   ~/.config/karabiner/karabiner.json: config/karabiner.json
    

# - create:
    # - ~/programming/tools/
    # - ~/.vim/undo-history

- shell:
  - 
    command: ./setup_homebrew.zsh
    stdin: true